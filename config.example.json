{
  "_comment": "Linux-Carla-MIDI-Daemon config. Strict JSON (no comments in your real file). Audio always follows the system default input/output — there is no audio config. See README for the full schema. Find MIDI names with: 'pw-link -o | grep -i midi'.",
  "samplers": [
    {
      "plugin": "BitsonicSampler",

      "midi": {
        "port": "events-in",
        "auto_add": false,
        "priority": [
          "USB func for MIDI",
          "CN29: Bluetooth"
        ]
      }
    }
  ],

  "auto_launch": {
    "enabled": true,
    "command": "carla $HOME/.config/carla-midi-daemon/BitsonicSampler.carxp",
    "process": "carla",
    "when_connected": ["USB func for MIDI", "CN29: Bluetooth"]
  }
}
